
// bool operators
! // not operator
== // equals operator
< // smaller than
> // bigger than
<= // smaller than or equal to
>= // bigger than or equal to
&& // and
|| // or

// integer operators
// sequencing points so that no undefined behaviour can occur (at every point of the code it is
// clear which value a variable has)
++i // pre increment increments the value first and then executes the rest of the code
i++ // post increment execudes the code first and after finishing increments the value
a + b // add
a - b // minus
a * b // multiply
a / b // divide
a % b // modulo
a += b // a = a + b
a = 1 | 8 // a = 9 bitwise or operator
a = 9 & 3 // a = 1 bitwise and operator
